# API Gateway URL
# For local development, the API gateway usually runs on localhost:8080.
# In Kubernetes, set NEXT_PUBLIC_API_URL at build time to the external URL
# (for example, https://dev.ai4inclusion.org).
NEXT_PUBLIC_API_URL=http://localhost:8080

# API Key for ASR, NMT, TTS services (required)
# This API key must have permissions: asr.read, asr.inference, nmt.read, nmt.inference, tts.read, tts.inference, pipeline.read, pipeline.inference, llm.read, llm.inference
# Created via: python3 create_api_key.py
NEXT_PUBLIC_API_KEY=ak_NbmAWzs-aFb3mdtb_Mh7rsnK0Vaci30_-saOyruntl4

# WebSocket URLs
# In Kubernetes, these should point to your external WebSocket endpoints
# (for example, wss://dev.ai4inclusion.org/asr or similar).
NEXT_PUBLIC_ASR_STREAM_URL=ws://localhost:8087/socket.io/asr
NEXT_PUBLIC_TTS_STREAM_URL=ws://localhost:8088/socket.io/tts

# Feature Flags
NEXT_PUBLIC_ENABLE_STREAMING=true
NEXT_PUBLIC_ENABLE_FEEDBACK=false

# Feature Flag Configuration (Unleash via Config Service)
# Environment name for feature flags (development, staging, production)
NEXT_PUBLIC_FEATURE_FLAG_ENVIRONMENT=development

# Feature flag timeout in milliseconds (default: 5000)
NEXT_PUBLIC_FEATURE_FLAG_TIMEOUT=5000
